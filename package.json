{
  "name": "ugc-koc",
  "version": "1.0.0",
  "private": true,
  "description": "UGC Manager para Hikvision LATAM — Admin + Iframe embed",
  "scripts": {
    "dev":         "cd src/admin && npm run dev",
    "build":       "npm run build:admin && npm run build:iframe",
    "build:admin": "cd src/admin && npm run build",
    "build:iframe":"cd src/iframe && npm run build",
    "install:all": "npm install && cd src/admin && npm install && cd ../iframe && npm install",
    "seed":        "npx tsx scripts/seed-videos.ts",
    "deploy":      "bash scripts/deploy.sh"
  },
  "devDependencies": {
    "tsx":       "^4.7.0",
    "typescript": "^5.5.0"
  }
}